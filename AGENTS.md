# AGENTS.md

このリポジトリでAIエージェントや開発者が作業するときの指示です。

## 最初に読むファイル

作業前に、次の順で確認してください。

1. `CONTEXT.md`
   - 用語集です。
   - 仕様書ではありません。
   - 用語の意味が曖昧なときだけ参照してください。

2. `README.md`
   - プロジェクト全体の概要と主要仕様です。
   - まずここで全体像を把握してください。

3. 関連する `docs/` ファイル
   - 体験やUIを変えるなら `docs/product/` と `docs/design/`。
   - 折り、切り、展開、状態管理を変えるなら `docs/engineering/`。
   - 大きな設計判断を変えるなら `docs/adr/`。

## このプロジェクトの最重要原則

### 1. 5歳児向けである

主ユーザーは5歳児です。
子ども向け画面には文字を出さず、アイコン、色、音、動きで操作を伝えてください。

追加するUIは、次の観点で必ず確認してください。

- 文字が読めなくても意味が伝わるか。
- 押せるものが多すぎないか。
- 押しても反応しない状態を作っていないか。
- 失敗やエラーに見える体験を増やしていないか。

### 2. 自由あそびである

このアプリはクイズや教材テストではありません。
正解、不正解、点数、ランキング、失敗表示を追加しないでください。

大事なのは、子どもが自由に触って、折る、切る、ひらく、模様が増える、を楽しめることです。

### 3. 見た目は3D、正確性は2D

Three.js / React Three Fiber は見せ方のために使います。
折り、切り、展開の正確性は、元の正方形の2D座標系を基準にしてください。

してはいけないこと:

- 3Dメッシュの現在の見た目だけを真実として扱う。
- 折り終わった紙を見た目優先で意図的にずらす。
- Raycasting結果をそのまま永続データにして、元の紙座標へ変換しない。

必ず行うこと:

- タップ位置を元の紙の2D座標へ変換する。
- 折り履歴を保持する。
- 切り抜きは元の紙座標に記録する。
- 展開結果は折り履歴の逆変換から求める。

### 4. 切るときは重なっている紙全部を切る

折った紙をタップしたとき、見えている一番上の面だけを切ってはいけません。
実物の折り紙切り絵と同じように、その位置に重なっているすべての紙面に切り抜きを適用してください。

画面上ではタップした場所に穴が1つだけ見えていても、内部データでは重なり分の切り抜きが発生します。
展開時に、それらが鏡写しで増えて見える必要があります。

### 5. アニメーション中は操作を受け付けない

折る、ひらく、新しい紙の切り替えなど、状態が動いている間はユーザー操作を無効にしてください。
押せないボタンを薄く表示するより、ボタン自体を一時的に消す方針です。

## 固定されたプロダクト仕様

変更するときは、先にドキュメント更新と合意が必要です。

- 対象は5歳児。
- 子ども向け画面は完全に文字なし。
- スマホ縦向き優先、タブレット対応。
- Webアプリ、PWA、オフライン対応。
- 初期紙色は水色。
- 選べる紙色は赤、水色、黄、緑、ピンク。
- 紙は正方形のみ。
- 折り回数は最大3回。
- 折り順は「左半分を右へ」「下半分を上へ」「左半分を右へ」。
- 切れるのは1回以上折ったあと。
- 一度切ったら、追加で折れない。
- 切ったあとも、図形を変えて最大10個まで切れる。
- 10個を超えたら、最も古い穴を消して新しい穴を入れる。
- 図形は丸、三角、四角、ハート、星。
- 図形サイズは固定。
- 図形は回転しない。
- 図形は端からはみ出して切れてよい。
- 完成後、紙タップには反応しない。
- 保存機能は最初の版には入れない。
- バイブレーションは使わない。

## UI実装ルール

- メイン操作ボタンは下中央に大きく置く。
- 補助アイコンは右上に小さく置く。
- 折る、ひらく、新しい紙は同じメイン操作位置に出す。
- 図形ボタンは、折ったあとだけ下部に横並びで出す。
- 図形ボタンは白い丸ボタンの中に濃いグレーのアイコンを置く。
- 選択中の図形は少し大きくする。
- 初期選択図形は丸。
- 色パレット中は、通常の操作ボタンを隠し、色ボタンだけを表示する。
- 色を選ばず戻る場合は、紙のあたりをタップして閉じる。

## 3D表現ルール

- 軽めの3Dを目指す。
- 紙のしなりや物理シミュレーションは最初の版では入れない。
- 折る途中は、折り線を軸に硬い板のように面を回転させる。
- 回転中には裏面の薄い同系色が自然に見えるようにする。
- 紙の外周には見た目だけ少し厚みを出す。
- 切り抜き穴の断面は表現しない。
- 重なりは、影、厚み、うっすら層の見た目で表現する。
- 光源とカメラは基本的に固定する。
- 完成時は正面から平らに見せる。

## 状態管理ルール

状態は明示的なステートマシンとして扱ってください。
少なくとも次の状態を区別することを推奨します。

- 初期状態
- 折りアニメーション中
- 折られた状態
- 切り可能状態
- 切り済み状態
- 展開アニメーション中
- 完成状態
- 新しい紙への遷移中
- 色パレット表示中

状態遷移時には、表示するボタンと受け付ける操作を必ず明確にしてください。

## テスト方針

優先してテストするもの:

- 折り変換が正しいこと。
- タップ位置が元の紙座標へ正しく変換されること。
- 重なっている紙全部に切り抜きが適用されること。
- 展開時に切り抜きが折り目に沿って鏡写しになること。
- 最大10個を超えたとき、最古の切り抜きが消えること。
- アニメーション中に操作を受け付けないこと。
- 状態に応じたボタン表示が正しいこと。

視覚的な3D表現よりも、幾何モデルと状態遷移の正確性を優先してテストしてください。

## ドキュメント更新ルール

仕様や設計を変えたら、コードだけでなくドキュメントも更新してください。

- 用語が増えたら `CONTEXT.md`。
- 体験や操作が変わったら `docs/product/`。
- 見た目、音、アニメーションが変わったら `docs/design/`。
- 幾何、状態、3D実装方針が変わったら `docs/engineering/`。
- 後から理由を説明する必要がある判断を変えたら `docs/adr/`。

ADRは何でも書く場所ではありません。
次の条件に当てはまる判断だけ追加してください。

- 後から変えにくい。
- 理由がないと不思議に見える。
- 複数の選択肢の間でトレードオフがあった。

## 変更前に確認すること

新しい機能や変更を入れる前に、次の問いに答えてください。

1. 5歳児が文字なしで理解できるか。
2. 押せるものが増えすぎていないか。
3. 折る、切る、ひらくの主体験を邪魔していないか。
4. 2D幾何モデルの正確性を壊していないか。
5. スマホでなめらかに動くか。
6. 既存ドキュメントの更新が必要か。

## 作業の区切り

意味のある単位で作業を止めたら、`npm run verify` を実行してください。

`npm run verify` は `fix` と `test` を順に回します。小さな変更でも、次の作業に移る前にこれを通して、見た目の崩れ、ルール違反、挙動の破綻を早めに見つけてください。

## 禁止事項

- 子ども向け画面に説明文を追加すること。
- 正解、不正解、点数、ランキングを追加すること。
- 折る方向を子どもに選ばせること。
- 折り順をランダムにすること。
- 切り抜きを見えている一番上の紙だけに適用すること。
- アニメーション中に状態変更を受け付けること。
- 完成後の紙タップに別機能を割り当てること。
- 保存、共有、親向けメニューを最初の版に勝手に追加すること。

## 推奨技術スタック

現時点の設計では、次を前提にしてください。

- React
- TypeScript
- Three.js
- React Three Fiber
- PWA対応
- 2D幾何モデルを独立したモジュールとして実装

具体的なパッケージ選定やビルドツールは、実装開始時に別途決めてください。
